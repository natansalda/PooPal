<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.android.pets"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pl.nataliana.mysporthabit.HabitActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="pl.nataliana.mysporthabit.AddActivity"
            android:label="@string/add_activity"
            android:theme="@style/EditorTheme"
            android:parentActivityName="pl.nataliana.mysporthabit.HabitActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.nataliana.mysporthabit.HabitActivity" />
        </activity>
    </application>

</manifest>