<?xml version="1.0" encoding="utf-8"?>
<manifest package="pl.nataliana.didipoo"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pl.nataliana.didipoo.HabitActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="pl.nataliana.didipoo.AddActivity"
            android:label="@string/add_activity"
            android:theme="@style/EditorTheme"
            android:parentActivityName="pl.nataliana.didipoo.HabitActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.nataliana.didipoo.HabitActivity" />
        </activity>
    </application>

</manifest>